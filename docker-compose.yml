version: '3.8'

services:
  db:
    image: postgres:16
    ports:
      - 5432:5432
    environment:
      DB_USER: postgres
      DB_PASSWORD: postgres
      DB_NAME: drjuris_db
    volumes:
      - db_data:/var/lib/postgresql/data
